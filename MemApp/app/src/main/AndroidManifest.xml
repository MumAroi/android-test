<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.paramas_wae.memapp">
    <!--READ_EXTERNAL_STORAGE : ขอสิทธิ์เข้าถึง พื้นที่จัดเก๊บข้อมูลภายนอกของ โทรศัพท์ **ไม่ใช่ sd card นะเห้ย -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!--WRITE_EXTERNAL_STORAGE : ขอสิทธิ์ในการเขียน พื้นที่จัดเก๊บข้อมูลภายนอกของ โทรศัพท์ **ไม่ใช่ sd card นะเห้ย -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>